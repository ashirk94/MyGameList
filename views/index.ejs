<a href='/donate' class="btn btn-donate"></a>
<div class="title">
    <h2>Recently Added</h2>
</div>

<div>
    <% games.forEach(game => { %>
        <a href="/games/<%= game.id %>">
            <img height="250" width="200" src="<%= game.imagePath %>">
        </a>
    <% }) %>
</div>
<br>
